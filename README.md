# vessl-runtime

ELION Lab VESSL 워크스페이스 런타임(bootstrap.sh · gpu_idle_watchdog.py · gpu_metrics_exporter.py).
정본은 private 저장소 `vessl_automation` 이고, 이 저장소는 `publish_runtime.sh` 가 자동으로 채운다 — **여기서 직접 편집하지 않는다.**
워크스페이스는 기동할 때마다 `stable` 태그의 세 파일을 받아 실행한다(bootstrap §0). 비밀값은 없다.

현재: vessl_automation@cf0420b 2026-09-21T15:18+0900
